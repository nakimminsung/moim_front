import React from 'react';
import QnaReportInsert from './QnaReportInsert';

function QNA(props) {
	return (
		<div style={{paddingLeft: '17%'}}>
			gg
			<QnaReportInsert
			// QnA 의 Num 값을 하위 컴포넌트로 보내야됨
			// qnaNum = {num}
			/>
		</div>
	);
}

export default QNA;
